>every API should be like:
api/versionNo/endpoint. Ex:api/v1/
>Dont use capital letters in API endpoints
>Only return minimum no. of fields/data in response, whatever is required
>API count should be minimal. If 2 or more APIs have same response with few fields, we can create single combined API
>If large list of items is being returned prefer pagination

>sending communications to users
>best selling products-based on order count
>implementing brands
>social auth
>non-login flow-order creation
>login mail verification-magic link, otp
>payment gateways integration
>microservice splitting
